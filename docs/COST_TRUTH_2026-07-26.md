# 비용 실측 — 2026-07-26 (읽기가 아니라 배포 잔재였다)

전제였던 "읽기 1.1만이 많다"를 실제 청구서와 대조한 결과, **읽기는 비용의 5%**였다.

## 1. 실제 청구 내역 (7/1~7/24, 합계 ₩958 / 7월 예상 ₩1,291)

Google Cloud 결제 보고서 SKU별 (절감액 -₩3,670 반영 후)

| SKU | 서비스 | 사용량 | 비용 | 비중 |
|---|---|---:|---:|---:|
| **Storage** | **Firebase Hosting** | 20.45 GB-month | **₩491** | **51%** |
| **Artifact Registry Storage** | Artifact Registry | 2.05 GB-month | **₩238** | **25%** |
| **Jobs** | Cloud Scheduler | 125 count | **₩158** | **16%** |
| Cloud Firestore Read Ops | — | 783,680 count | ₩49 | 5% |
| Cloud Run Functions egress | — | 0.06 GB | ₩11 | 1% |
| Standard Storage Seoul | Cloud Storage | 0.18 GB-mo | ₩6 | — |
| Outgoing Bandwidth | Firebase Hosting | 3.61 GB | ₩3 | — |

**핵심: 읽기 78만건에 ₩49.** 읽기를 절반으로 줄여도 월 ₩25 절약이다.
반면 상위 3개(₩887, 92%)는 **학급 수와 무관하고 배포 횟수에 비례**한다.

## 2. Hosting 저장용량의 정체 — ✅ 조치 완료

Hosting API 조회 결과 **버전 2,265개 / 30.35 GB**. 각 버전 85.1 MB(336파일)이고
실제로 서빙되는 건 최신 1개뿐, 나머지 2,264개는 전부 배포 이력이었다.
배포 1회 = +85 MB ≈ **월 +₩2**가 영구히 누적되는 구조.

### 실행한 조치
1. **보관 정책 설정** — `PATCH /v1beta1/sites/{site}/config?updateMask=maxVersions` → `maxVersions: 10`.
   프로젝트의 사이트 3개(`inconomysu-class`, `-85a88`, `-ad1cf`) 전부 적용. 이후 배포는 자동 정리된다.
2. **누적분 정리** — 라이브 릴리스가 가리키는 버전 + 최신 15개를 보호목록으로 잠그고
   나머지 2,250개 삭제. (사이트 `-85a88`, `-ad1cf`는 버전 0개라 대상 없음)

**결과: 30.35 GB → 1.25 GB** (보관 15개). 무료 한도 10 GB 안으로 복귀.
라이브 사이트 HTTP 200 정상 확인.

> ⚠️ 함정: Hosting API는 초당 쓰기 쿼터가 있어 동시 10으로 삭제하면 **429가 대량 발생**한다
> (1차 시도 959건 실패). 동시 3 + 요청당 120ms + 429 지수 백오프로 재시도해 전량 성공.

부수적으로 버전 1개가 85 MB인 것도 크다(아바타·재판방 PNG 에셋). 에셋 최적화는 별건.

## 3. Artifact Registry 2.05 GB — ✅ 조치 완료

Cloud Functions를 배포할 때마다 빌드 컨테이너 이미지가 `gcf-artifacts`에 쌓인다
(asia-northeast3 1.81 GB, us-central1 소량).

`PATCH /v1/projects/.../repositories/gcf-artifacts?updateMask=cleanupPolicies,cleanupPolicyDryRun`로
정리 정책 적용:
- `keep-recent`: `action=KEEP`, 최근 3개 버전 보존
- `delete-old`: `action=DELETE`, 7일(604800s) 경과분 삭제
- `cleanupPolicyDryRun=false` (실제 삭제 모드)

> ⚠️ 함정: `mostRecentVersions`만 주고 `action`을 빠뜨리면 400 `invalid cleanup policy action`.
> KEEP 정책에도 `action: "KEEP"`을 명시해야 한다.

정리는 비동기로 수행되므로 용량 감소는 하루 정도 뒤 반영된다.

## 4. Cloud Scheduler ₩158

앱이 정의한 onSchedule 함수는 4개이고 3개까지 무료다.
※ firebase CLI 토큰으로 `cloudscheduler.jobs.list`가 전 리전 0개를 반환했다(권한 부족 추정) —
   실제 개수는 Cloud 콘솔에서 직접 확인할 것. 과거 배포의 고아 작업이 남아있는지 점검 가치 있음.

## 5. 그래도 읽기를 줄인 이유 — 무료 한도

읽기는 돈이 아니라 **무료 한도(5만/일)** 가 문제다. Cloud Monitoring 실측:

| 날짜 | 읽기 | |
|---|---:|---|
| 7/13(월) | 39,100 | |
| 7/14(화) | **51,388** | ⚠️ 무료 한도 초과 |
| 7/20(월) | 38,941 | |
| 7/23(목) | **55,903** | ⚠️ 초과 |
| 7/24(금) | 35,636 | |
| 7/19(일) | 1,254 | 무사용일 기준선 |

**사용 규모 = 2학급 40명**(사용자 확인, 방학 전 기준). 7/23 KST 시간대별로 보면
09~13시가 45,499(64%) — 순수 수업 중 사용량이다.
→ **학생 1명당 하루 ≈ 1,775읽기**, **학급 1개(20명)당 ≈ 35,500/일**.

### 단가 (Cloud Billing Catalog API로 확인한 실제 SKU)
`Cloud Firestore Read Ops Seoul` = **$0.00000038/건 = $0.38 / 100만건**
(흔히 인용되는 $0.06/10만은 US 멀티리전 기준. 서울은 $0.038/10만으로 더 싸다.)
무료 한도 = **5만 읽기/일**.

### 학급 수별 전망 (현재 코드 기준, ₩1,400/USD)

| 규모 | 일 읽기 | 과금분 | 월 비용 |
|---|---:|---:|---:|
| 2학급 40명 (현재) | ~71,000 | 21,000 | **~₩330** |
| 10학급 200명 | ~355,000 | 305,000 | ~₩4,800 |
| 50학급 1,000명 | ~1,775,000 | 1,725,000 | ~₩27,000 |

즉 "학급을 늘려도 괜찮은가"의 답은 **괜찮다** — 다만 무료 한도는 2학급에서 이미 소진되므로
그 이후는 전부 종량 구간이고, **학생 1명당 읽기를 줄인 만큼 그대로 곱해서 절약된다.**
읽기 절감 작업의 가치는 지금이 아니라 확산 시점에 나온다.

> 정정: 초판에서 "10학급이어도 월 ₩500"이라고 적었는데, 이는 청구서의 모호한
> `Cloud Firestore Read Ops / App Engine ₩49` 줄(결제계정에 프로젝트 6개가 묶여 있어
> 이 프로젝트 몫인지 불확실)로 역산한 값이었다. 위 표는 공식 SKU 단가 기준이다.

## 6. 이번에 고친 것 — 방치된 탭의 백그라운드 읽기

Cloud Monitoring 5분 해상도로 새벽(무사용) 구간을 보니 규칙적인 버스트가 있었다.

```
01:15=89  02:15=3  03:15=3  04:15=88  05:15=2  06:15=88  07:15=3  08:20=87
```

**2시간 주기 ~88읽기 + 1시간 주기 ~3읽기.** 아무도 안 쓰는데 탭만 열려 있어서 나는 읽기다.

원인은 `setInterval` tick에 visibility/idle 가드가 없는 폴러들이었다.
`usePolling`은 이미 가드가 있었지만(숨김·무조작 시 skip), 개별 컴포넌트의 폴러는 빠져 있었다.

| 위치 | 주기 | 1회 읽기 |
|---|---|---|
| Dashboard `pollData` | 2시간 | jobs 47 + commonTasks 9 = **56** (CLASS2025 기준) |
| Dashboard `checkDateAndRefresh` | 1시간 | ~3 |
| VoteReminderBanner | 15분 | 법안 쿼리 + userVotes |
| NewBillPopup | 30분 | 위와 **동일한 쿼리**(전 페이지 상주라 모든 탭에 영향) |
| RealEstateRegistry 설정/매물 | 15분 | 매물 20~25 |

### 수정 (커밋 `1cfa391`)
`src/utils/backgroundPoll.js` 신설 — `usePolling`이 쓰던 규약을 헬퍼로 추출:
- tick은 `visibilityState === "visible" && !getIsIdle()`일 때만 실행
- 탭 복귀/조작 재개 시 마지막 조회가 `resumeGapMs`보다 오래됐으면 1회 즉시 조회

초기 1회 조회는 호출부가 그대로 직접 수행하므로 **첫 로드는 무영향**.
화면을 실제로 보는 순간엔 복귀 조회로 갱신되므로 **표시 신선도는 종전과 동일**.

라이브 검증: 대시보드 할일(직업+공통) 정상 렌더, 부동산 20건·설정값 정상, 콘솔 에러 0.

## 7. 남은 후보 (7일 상위 쿼리 기준)

| 쿼리 | 실행 | 읽기(7일) |
|---|---:|---:|
| `/storeItems WHERE…` | 1,075 | 16,494 |
| `/CentralStocks` (2종) | 641×2 | 28,204 |
| `/users/*/inventory` | 1,072 | 8,660 |
| `/users` (**필터 없음**, 88문서) | 34 | 2,992 |

- **CentralStocks**: `updateRealStockPrices()`(isRealStock+isListed)와
  `updateCentralStocksSnapshot()`(isListed)이 5분 스케줄마다 각각 컬렉션을 읽는다.
  필터가 달라 단순 중복은 아니지만, 앞 함수가 이미 메모리에 들고 있는 데이터를 넘겨주면
  절반으로 줄일 수 있다. **다만 주식=학생 자산 파이프라인이라 중대 변경 절차(FULL) 필요.**
- **storeItems/inventory**: ItemContext CF. 30분 폴링 × 학생 수가 원인이며 구조상 정상 동작.
  줄이려면 CF를 "카탈로그"와 "마켓"으로 쪼개야 하는데, 선물·관리자 지급으로 인벤토리가
  외부에서 바뀔 수 있어 staleness 위험이 있다. 권장하지 않음.
- **`getAllUsersDocuments`** (`src/firebase/db/users.js:252`): 필터 없이 전 학급 88명을 읽는다.
  앱 내 직접 호출부는 없고 re-export만 있으므로 제거 후보.

## 7-1. 주식은 비용의 얼마인가 (검증 결과)

**결론: 수업일 읽기의 약 6%. 큰 축이 아니다.**

- 스케줄러 **1회 실행 = 정확히 44읽기** (가격갱신 `isRealStock+isListed` 22 + 스냅샷 `isListed` 22).
  7/24 새벽 5분 해상도에서 `06:05=44 06:35=44 07:05=44 07:35=44`로 실측 확인.
- 하루 ~92회 실행 → **~4,030읽기/일**. 7/23(70,981) 대비 5.7%, 7/24(35,636) 대비 11%.
- **학생 쪽은 이미 최적화돼 있다.** 클라이언트는 CentralStocks를 직접 읽지 않고
  CF 스냅샷 → `Settings/centralStocksCache` **1문서**만 읽는다(폴백만 전량 조회).
  → **종목 수를 늘려도 학생 읽기는 안 늘어난다.** 종목 수는 서버 스케줄러 비용에만 영향.
- 스케줄러 게이트는 이미 촘촘하다: 주말·장외 0읽기, 06~08시는 30분 게이트로 4회,
  그 외 시간엔 `Settings/activeStatus`로 최근 70분 내 활성 사용자 없으면 skip.
- GHA `scheduler.yml`은 `workflow_dispatch` 전용이라 **중복 실행 없음**.

### 적용: 5분 → 15분 (커밋 `b6f8963`, 배포 완료)
하루 4,030 → **약 1,340읽기**.
⚠️ 원래 설계가 15분이었다 — `realStockService.js` 헤더 주석과 **학생에게 보이는 UI 문구**
("15분마다 자동으로 가격이 업데이트됩니다", `StockExchange.js:376,508`)가 모두 15분 기준이라
`*/5` cron이 오히려 안내와 불일치였다. 이번 변경으로 표기와 실제가 일치한다.
미국장 창은 `minute % 30 >= 5` 게이트가 있어 `*/15`에서도 0분·30분만 통과 → 종전과 동일한 4회.

### ETF·채권 정리? — 비권장
- **전제 정정: 22종목 전부 `isRealStock: true`다.** 채권(KOSEF 국고채10년)도 ETF 7개도
  전부 실제 상장 티커에 연동돼 있다. "실주식 연동이 아닌 상품"은 존재하지 않는다.
  - stock 14 / etf 7(QQQ·TLT·GLD·SPY·KODEX 200·TIGER 미국S&P500·KODEX 레버리지) / bond 1
- 학생 보유 76포지션 중 **ETF·채권 보유 0건** → 내려도 자산 파괴 위험은 없다.
- 그러나 8개를 빼도 44 → 28읽기/회, 15분 기준 하루 1,340 → 850. **전체의 0.7% 절감.**
  분산투자·안전자산 학습 소재를 0.7% 때문에 없앨 이유는 없다고 판단.
- 굳이 한다면 문서 삭제가 아니라 `isListed: false` (되돌리기 쉽고, 보유자 0이라 부작용 없음).

## 7-2. 별건 — 포트폴리오 고아 포지션 54건

`portfolio` collectionGroup 전수 조회: **76건 중 54건이 현존하지 않는 stockId를 가리킨다**
(`delistedAt` 표시도 없음). 학급별 CLASS2025 42건 / classCode 없음 18건 → 지난 학년도 잔재로 추정.
비정상 수량도 있다(롯데리츠 184,886주, 우리반 미세먼지 5,555주, 한국전력공사 1,665주 등).

`netAssets.js:178`은 `stocks.find(...)`로 매칭 실패 시 **해당 보유분을 0원 처리**한다.
따라서 이 54건은 순자산에 전혀 잡히지 않는다(상장폐지 처리로는 타당하나, 의도된 폐지인지 확인 필요).
**학생 자산이라 임의 삭제하지 않았다.**

## 8. 결론

1. **비용 걱정의 대상이 틀렸다** — 청구의 51%는 Hosting 배포 버전 이력(₩491), 읽기는 5% 이하.
2. ✅ Hosting 버전 30.35 GB → **1.25 GB** (보관 10개 정책 + 누적 2,250개 정리).
   ✅ Artifact Registry 정리 정책 적용(최근 3개 유지 / 7일 경과분 삭제).
   → 월 ₩729(청구의 76%)가 사라질 전망. 다음 청구서에서 확인할 것.
3. 읽기는 **무료 한도(5만/일)** 때문에 여전히 줄일 가치가 있다. 2학급 40명이 이미 한도를
   넘나들고, 학급을 늘리면 학생 1명당 읽기가 그대로 곱해진다.
   이번 폴러 가드로 "방치된 탭이 만드는 읽기"는 0에 수렴한다.
   효과는 **다음 수업일** Monitoring 그래프로 확인(주말 수치는 대표성 없음).
